rust-wasm-algo-lab/
│
├── src/                          ← Rust (unchanged, keep everything)
│   ├── lib.rs
│   ├── grid.rs
│   ├── stats.rs
│   ├── algorithms/
│   │   ├── mod.rs
│   │   ├── bfs.rs
│   │   ├── dfs.rs
│   │   ├── dijkstra.rs
│   │   └── astar.rs
│   └── maze/
│       ├── mod.rs
│       ├── recursive_backtracker.rs
│       └── prims.rs
│
├── pkg/                          ← wasm-pack output (gitignored)
│   ├── rust_wasm_algo_lab.js     ← JS bindings, no hash
│   ├── rust_wasm_algo_lab_bg.wasm
│   └── rust_wasm_algo_lab.d.ts
│
├── web/
│   ├── app.js                    ← same app.js, fixed imports
│   └── style.css                 ← unchanged
│
├── index.html                    ← hand-written, loads pkg/ directly
├── Cargo.toml                    ← unchanged
└── .gitignore                    ← add pkg/ and target/

# Reminder that Gitignore cleanup is with:
# git rm -r --cached [folder name]
# git commit -m "remove dist and pkg from tracking"
# git push