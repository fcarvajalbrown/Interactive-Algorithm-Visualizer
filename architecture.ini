rust-wasm-algo-lab/
├── Cargo.toml
├── Trunk.toml
├── index.html
├── README.md
│
├── src/                          # Rust/WASM core
│   ├── lib.rs                    # wasm-bindgen entry point, exports
│   ├── grid.rs                   # Grid struct, cell types (Empty, Wall, Mud, Water)
│   ├── algorithms/
│   │   ├── mod.rs
│   │   ├── bfs.rs
│   │   ├── dfs.rs
│   │   ├── dijkstra.rs
│   │   └── astar.rs
│   ├── maze/
│   │   ├── mod.rs
│   │   ├── recursive_backtracker.rs
│   │   └── prims.rs
│   └── stats.rs                  # NodesExplored, PathLength, ExecutionTime
│
└── web/                          # Vanilla JS + CSS (no framework)
    ├── main.js                   # App entry, wires WASM to canvas
    ├── renderer.js               # requestAnimationFrame loop, canvas painting
    ├── input.js                  # Mouse/touch/keyboard event handlers
    ├── comparison.js             # Split-screen logic (P1)
    └── style.css

# Reminder that Gitignore cleanup is with:
# git rm -r --cached [folder name]
# git commit -m "remove dist and pkg from tracking"
# git push