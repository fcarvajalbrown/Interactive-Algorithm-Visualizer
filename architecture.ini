rust-wasm-algo-lab/
│
├── src/
│   ├── main.rs                   ← macroquad entry point, game loop
│   ├── grid.rs                   ← mostly unchanged, remove wasm_bindgen
│   ├── stats.rs                  ← unchanged
│   ├── ui.rs                     ← sidebar buttons and stats rendering
│   ├── algorithms/
│   │   ├── mod.rs                ← unchanged
│   │   ├── bfs.rs                ← unchanged
│   │   ├── dfs.rs                ← unchanged
│   │   ├── dijkstra.rs           ← unchanged
│   │   └── astar.rs              ← unchanged
│   └── maze/
│       ├── mod.rs                ← unchanged
│       ├── recursive_backtracker.rs ← unchanged
│       └── prims.rs              ← unchanged
│
├── Cargo.toml                    ← swap wasm-bindgen for macroquad
├── Cargo.lock
├── index.html                    ← macroquad WASM template (minimal)
└── .gitignore

# Reminder that Gitignore cleanup is with:
# git rm -r --cached [folder name]
# git commit -m "remove dist and pkg from tracking"
# git push